layout :default, :title => 'トリック一覧' do
  h2 { a "トリック一覧", :href => "/tricks" }

  ul do
    @tricks.each do |trick|
      li { a trick.name, :href => "trick/#{trick.id}" }
    end
    li {
      small {
        a "(トリックを追加する)", :href => "/trick"
      }
    }
  end

  a "トップ", :href => "/"
end
