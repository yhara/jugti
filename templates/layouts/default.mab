doctype :html4_strict

html do

  head do
    title "#{@title} - Jugti"
    link :href => '/css/site.css', :rel => 'stylesheet', :type => 'text/css'
  end

  body do
    h1 { a "Jugti", :href => '/' }
    div.main do
      layout_content
    end
  end

end

